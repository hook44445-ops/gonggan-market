# 인계지시서 11차 — 2026-09-26 저녁 (10차에 이어서)

받는 사람: 다음 세션 Claude. 대표는 김태웅, 호칭은 「대표」. **보고·안내는 모두 한국어.**
운영 https://gongganmarket.com
앞 문서: … → 10차 `docs/HANDOFF-2026-09-26-e2e-10.md` → **이 문서(11차, 최신)**.
10차 5·6·7-4장(방법·함정·규칙·작업 폴더)은 그대로 유효 — 먼저 읽어라.
작업 폴더: worktree `D:\project\gonggan-fix-0926b`. 메인 폴더 `D:\project\gonggan-market` 은 건드리지 않는다. 새 일은 origin/main 에서 새 브랜치.

---

## 0. 대표 체크 칸 (대표 「내가 할 건 인계지시서에 남겨 주고」 · 「시간 날 때 작업할 수 있게 체크」)

코드는 전부 «키만 넣으면 켜지게» 들어가 있다. 키가 없으면 예전과 똑같이 돈다.
넣는 곳: **Vercel › 공간마켓 프로젝트 › Settings › Environment Variables (Production)** → 넣은 뒤 **Redeploy 한 번**.

### 0-1. 라운지 AI 글쓰기 (#783)
- [ ] **`OPENROUTER_API_KEY`** — 플랜 A(유료, 최신 Sonnet). **프루비 Vercel 에 같은 이름으로 있다 → 값 복사.**
- [ ] **`GEMINI_API_KEY`** — 플랜 B ① 무료. aistudio.google.com › Get API key.
- [ ] **`GROQ_API_KEY`** — 플랜 B ③ 무료. 프루비에 있으면 복사, 없으면 console.groq.com.
- [ ] (선택) `LOUNGE_LLM_ORDER` — 순서 바꾸기. 무료만 쓰려면 `gemini,openrouter_free,groq`. 기본은 `openrouter,gemini,openrouter_free,groq`.
- [ ] (선택) `LOUNGE_LLM_MODEL`(플랜 A 모델 고정) · `GEMINI_MODEL` · `GROQ_MODEL` · `LOUNGE_FREE_MODEL`.
- 확인: 관리자 › 콘텐츠 AI › **AI 운영본부** 맨 위 「라운지 자동 글쓰기 AI **· 연결됨**」, 조직도 가동 직원 수 올라감.
- 비용 감(플랜 A): 글 한 편 약 20~30원, 하루 5편 → 한 달 약 5천 원 안팎(OpenRouter 청구서로 확인).

### 0-2. 뉴스·날씨 글 (#775 — 10차 7-2 그대로)
- [ ] **`NAVER_CLIENT_ID` · `NAVER_CLIENT_SECRET`** — 「공간 뉴스」 하루 1건. developers.naver.com › 검색 API.
- [ ] **`KMA_SERVICE_KEY`**(일반 인증키 Decoding) — 「날씨와 집」. data.go.kr › 기상청_단기예보 조회서비스.

### 0-3. 머지 · 운영 SQL
- [ ] **#783 머지**(이 문서 포함). SQL 없음.
- [ ] 머지 뒤 Claude 가 운영 확인: AI 운영본부 연결 카드 · 사람 › 고객 맨 위 토큰·온도 조정.

### 0-4. 대표 답 대기
- 보물 기능 A(장터)/B(이벤트) — 메모리 `gonggan-treasure-feature-plan`.
- 토스: 지급 방식(에스크로/지급대행) 결정.

---

## 1. 10차 뒤 한 것 (09-26 오후~저녁)

| PR · SQL | 내용 | 운영 |
|---|---|---|
| #776~#778 · **143** | 결제 취소·환불 서버 기록 · **143 ⓪ `payment_orders` 에 031 칸이 운영에 없던 것 추가** · 금액 칸 bigint → numeric | ✅ 143 + 보완 true |
| #778 | 관리자 사용자 찾기 서버로(`/api/admin/users` action `lookup` — 번호·ID) · 로그인 인증번호 「다시 요청」 안내 · 확인 버튼 로딩 | ✅ +20 토큰(잔액 50) |
| #779 | 새 마이(v3)에 「운영」 입구(관리자 화면 · 운영자 게시판 관리) · 토큰 화면 들어갈 때 새로 읽기 | ✅ |
| #780 | **관리자 메뉴 정리 1차** — 7묶음(오늘·사람·거래·돈·공사 증빙·라운지·신고·콘텐츠 AI·설정) · 첫 화면 「오늘 할 일」 · 긴급 스위치 위로 · 개발 토글은 설정 안 · 쓰지 않는 AI 화면은 설정 › 「실험:」 | ✅ |
| #781 · **144** | 댓글 숨김 `op_set_comment_hidden` 을 토큰 사용자로(남의 id 로 숨김 막음) | ✅ 144 true · [점검] 댓글 2개 숨김 |
| **138** | 운영 실행 | ✅ true |
| #782 | **라운지 카테고리 주제**(부동산·건강·연애·맛집·주식·반려동물·취업·생활·결혼·운동·여행·동네·유머·자유) 주제·작성기 · 「폭우과(와)」 등 조사 고침 | 머지 |
| #783 | **라운지 AI 글쓰기**(아래 2장) · AI 조직도 최신 모델 · 토큰·온도 조정 한 곳(사람 › 고객) | **머지 대기** |

## 2. 라운지 AI 글 — 지금 구조 (#783)

- `src/lib/serverLoungeWriter.js` — 서버 전용. 차례: OpenRouter 유료 → Gemini 무료 → OpenRouter `:free` → Groq → 없으면 null(틀 글).
  프롬프트: 제목 16~34자(검색어 앞 · 클릭하고 싶게 · 낚시 금지) · 첫 줄 「한 줄 답」(AEO) · 소제목 4~6(질문형 2+) · 체크리스트/표 · 자주 묻는 질문 3~4 · 지역(GEO) · 수치 지어내기 금지 · 건강/주식 안전.
  검사 탈락(1,200자 미만·소제목 3 미만·FAQ 없음·링크·제목 길이) → 다음 차례.
- `src/lib/openRouterModels.js` — OpenRouter 공개 목록(키 불필요)에서 **역할별 최신 모델**(6시간 캐시). 조직도·추천·글쓰기 공통. `setServerLlmStatus`(aiOrg) 로 관리자 화면에 반영.
- `api/trend/check-trends?mode=llm_status` — 연결 여부·차례·모델 이름만(비밀 없음). `vercel.json` 에 이 함수 maxDuration 60.
- 자율 사이클: 주제 = **공간(수요자·공급자) 4 : 라운지 카테고리 2**(대표 「인테리어 수요자·공급자 글 특히 자주」). AI 글은 `ai_source='server_llm'`. **AI 가 연결되면 틀 글은 자동 발행 안 함**(초안).
- 구글·네이버: 봇에게 prerender(요약·Article·FAQPage) 이미 나감 — AI 글 형식이 여기에 맞춰져 있다.

## 3. 남은 것 (중요 순)

1. ~~사진 품질~~ **했음(#783)** — 힉스필드 고화질 41장 `public/images/lounge/<카테고리>-N.webp`(얼굴·글자 없음) · `approvalImage.js` 가 글의 라운지 카테고리 사진을 먼저 고른다(뉴스·큐티 등 브랜드 칸은 그대로). 이미 발행된 옛 글 사진은 그대로 — 바꾸려면 관리자에서 글별로. 힉스필드 크레딧 약 19 남음.
2. **옛 AI 초안 정리** — 7월 초안 23건 · 9/13 예약 「부동산 대책과(와) 공간의 관계」 · 에디터 추천의 「폭우과(와)」 제목. **숨김/반려로만**(영구 삭제 금지).
3. 관리자 정리 3차 — 공사 증빙·대화·직거래 세 화면 점검 + 인쇄(분쟁 증빙 한 장) · 거래 쪽 인쇄 · 힉스필드 그림. 마지막에 관리자 전체 점검(메모리 `gonggan-admin-todo`).
4. **견적 요청·진행에 «조명» 추가**(대표 메모 — 메모리 `gonggan-quote-lighting-todo`).
5. 결제 2단계 — 토스 가상계좌 웹훅 · 업체 실제 지급(대표·토스 결정 뒤).
6. **프루비**: 대표 「구글 플레이 journal 1000만 다운 앱(컬러노트·똑똑노트) 벤치마킹해 프루비에 추가 구현」 — 프루비 저장소(`D:\project\prubi`)는 다른 창이 쓸 수 있다 → 워크트리로.
7. 나중: 마케팅 ASO · 원격 미해결 3건 · 플레이 테스터 12명 · 스토어 빌드 · `gonggan-fix-0924c` 워크트리 정리(5183·5187 서버 꺼진 뒤).

## 4. 방법 · 함정 (새로)
- 워크트리 git 이 「dubious ownership」 을 내면 명령마다 `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0='*'`(gh 도 같은 셸에서 export 해야 함).
- 대표가 작업 중간에 여러 줄로 이어 말한다 — 한 덩어리 지시로 묶어 처리하고, «메모»라고 하면 메모리에만 적고 넘어간다.
- 서버 AI 키 이름은 **VITE_ 로 만들지 않는다**(브라우저 번들에 들어간다). 옛 `llmClient.js`(VITE_LLM_API_KEY)는 브라우저용 — 쓰지 않는다.

## 5. 규칙 (10차 6장 그대로)
- 보고 한국어 · SQL 은 전체 원문 + 확인 칸 수 · 머지·배포·운영 SQL·메일 발송은 대표만 · 영구 삭제 금지 · 키는 대표만 · SQL 122 실행 금지 · 메인 폴더 금지 · 맨 `git stash` 금지.
