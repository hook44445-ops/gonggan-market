# 인계지시서 — 공간마켓 09-24 밤 (총점검 수정 3차)

받는 사람: 다음 세션 Claude. 대표는 김태웅, 호칭은 「대표」. 보고·안내는 모두 한국어.
운영 https://gongganmarket.com
앞 문서: `docs/HANDOFF-2026-09-24-e2e.md`(1차) → `docs/HANDOFF-2026-09-24-e2e-2.md`(2차) → **이 문서(3차, 최신)**.
점검 항목 전체(A/B/C/D 번호)는 `docs/CHECK-2026-09-24-e2e.md` — 1·2차 문서와 점검 문서는 메인 폴더 `D:\project\gonggan-market`(브랜치 `docs/handoff-0924-b`, 다른 창 작업 중·커밋 안 된 파일 있음 → **건드리지 않는다**)에 있다.

---

## 0. 지금 상태 (09-24 밤, 여기서 멈춤)

| 항목 | 상태 |
|---|---|
| main | PR **#713 까지 머지·배포됨**(대표 「머지하고 이어해」) |
| 작업 브랜치 | **`fix/double-pay-guard`** — 작업 폴더 **`D:\project\gonggan-fix-0924c`**(git worktree). 커밋 19개, **푸시 안 함 · PR 없음** |
| 운영 SQL | 106 · 107 · 108~111 · **112 실행됨**. **pg_cron 켜짐**(대표 확인 `true`) — `escrow_auto_approve_hourly`(매시 7분) |
| 운영 SQL 미실행 | **113**(`supabase/migrations/113_chats_read_at_fee_rules.sql`, 이 브랜치) — 대표가 실행 |
| 테스트·빌드 | 이 브랜치 **마지막 확인은 C17 커밋 때(테스트 165 통과 · 빌드 OK)**. 그 뒤 커밋은 아직 안 돌림 |
| 앱 빌드(스토어) | 대표: **「빌드는 완전히 다 점검하고 수정 후에 하면 돼」** → 아직 안 함 |
| 토스 | 운영 키가 테스트 키(`test_ck_…`) — 실제 돈 안 나감 |

---

## 1. 대표가 한 말 — 어제(09-23)·오늘(09-24)

### 정책·방향
- **「베타라도 정식 서비스 하는 것처럼 진행」** — 결제 단계 유지. 결제창·결제 정보·약관 동의는 대표.
- 한도(에스컬레이션): 「1,000만원 초과부터는 보증금 들어가는 거 아님?」 「에스컬레이션이 헷갈릴 수 있는 부분 정리」 **「일단 1안 위주, 2안은 문의 시로 — 2안이 생기면 기본 규정이 헷갈려짐」** 「쉽게 가면 1,000만원까지 시공보험·사업자등록증으로 커버, 그 이상은 보증금 프리미엄 파트너」 **「가입 문턱을 낮춰서 에스컬레이션으로 가면 된다」**
- 공사대금 단계(A3): 「0원~500만원 2단계, 500만원 이상~1억 4단계」, 사업자 없는 업체는 완료 때 100%, 사업자 있으면 착공 30% — 「응 그렇게 가자」(«내면 유리한» 구조). 자동 승인은 서버에서(48시간 제안).
- 채팅: 고객이 견적 요청 → 업체 견적 → **고객이 선택한 순간 방이 열린다**, 선택 뒤 전화번호 공개(「가 진행」). 「채팅하고 연락하고 현장방문하고」.
- 「고객의 견적요청 번복 자체는 매너온도를 결정하는 요소」(A4).
- 「공정 과정이 충분히 상호간의 신뢰가 느껴지는 구조로」.

### 아이콘·디자인
- 아이콘: 「녹색이 아동복지 같다, 프리미엄 인테리어 비교견적(중개) 느낌이 아니다」 → 「그냥 현재에 배경색만 바꾸는 방향은?」 → **「더 깊은 초록이나 네이비로」** = 로고(집·마주한 두 사람·열린 문) 그대로, 배경만 `#0E2B1D`(#705 반영).
- 「구조·구성·배경 및 UX/UI 까지 힉스필드로」 — 기준 화면은 「내 한도 · 서류」.
- 「총점검하면서 힉스필드 구조 보정해야 할 곳들도 점검」 「**최종견적서 확인** 쪽도 힉스필드로 구조적으로 보기 좋게」 「**결제 수단 선택** 구간도」 「채팅창 안에서 구조적 힉스필드 쓸 곳 점검」.

### 총점검
- 「의뢰인 입장에서 견적 신청부터 에스크로 결제·리뷰 후기까지. 파트너는 받아 주면서. 관리자 아이디로. 시스템 총점검」 「너가 직접 사용하면서 총평」.
- 「01027406030 계정 — 관리자·의뢰인·파트너 다 들어가 있어」 「로그아웃하고 다시 들어가면 돼」.
- 「오늘 9월 24일 일자로 테스트」 「업체·의뢰인 입장 UI/UX 편의성·시스템 구조 제대로 작동 등 총점검」 「구간별 GPS 점검도」 「에스크로 단계 고객·업체 채팅 점검」.
- 「총점검하면서 **스토어 이미지와 ASO** 에 넣을 만한 내용까지 보관」.
- 푸시: 「종이·에스크로·라운지 채팅 등 iOS·구글 앱 푸시 연결할 곳들 연결해」 「관리자 페이지까지」(#704).

### 진행 지시
- 「머지」「머지 후 진행」「머지해」 「추천순으로 진행해」 「인계지시서의 할 것 체크해서」 「내부 브라우저로 체크」.
- (오늘 밤) 「pr 713까지 진행됐고 이어하다 끊김」 → 「112 원문 주고」 → 「pg_cron_on = false 나옴」 → 「적용했고 true 나왔고 **머지하고 이어해**」 → **「빌드는 완전히 다 점검하고 수정 후에 하면 돼」** → 「여기서 멈추고 인계지시서부터」.

---

## 2. 완료된 것

### 머지·배포됨 (main)
- **#704** 프리미엄 파트너 · 푸시 배선(수신설정 16종 · 즉시 발송 · 관리자 푸시 현황) · 아이콘 v4
- **#705** 베타 보관·에스크로 약속 문구 20여 곳 분기 · 관리자 「수동 인증」 제거 · **아이콘 v5**(배경 `#0E2B1D`)
- **#706** 면허 서류 칸 · **「내 한도 · 서류」 화면**(힉스필드 엠블럼·배경) · 입찰 한도가 면허를 못 보던 버그
- **#707** 「이 업체로 선택」 멈춤(React #300) · 파트너센터 입찰 불가 · 진행중 42703 · 한도 계단 1안 정리
- **#708** 착공 사진 «업로드 실패» 표시 해결
- **#709~#712 + SQL 103~105** 공사 한 건 = 대화방 하나 · 방 갈라짐 · 상태 멈춤 · 최종견적 알림 누락 · GPS 읽기 400
- **#713 + SQL 106~112** (다른 창) D4 숨김요청 · D11 옛 정리 코드 · D12 파트너 내정보 연결 · D18 대화 탭 · D2 운영 스위치(ops_config) · D5 토큰 적립 · D6 새 요청 파트너 알림 · D8 진행건 조회 · D9 요청 수정 저장 · D10 · D13 신뢰평가 중복 · C16 · **C28 공간온도 서버로** · **C29 자동 승인 서버(pg_cron)** · **C30/A3 단계 구간** · C26/C27 SQL · D1 users 정책

### 이 브랜치 `fix/double-pay-guard` (커밋됨 · 푸시 전)
| 커밋 | 내용 |
|---|---|
| f6c3283 | **C17 이중 결제 막기** — 주문번호 `gm_{요청ID}_{시각}`, 서버(`api/confirm-payment.js`)가 이미 PAID 면 409 `ALREADY_PAID`(토스 승인 안 함), 화면은 「이미 결제된 공사 · 공사 화면 보기」 |
| af70e63 | **C1** 비교 목록 업체 정보 기본값 · 확인 안 된 「✓ 공간보증」 칩(→ `isGuaranteeBadgeVisible`) |
| dd9781f | **B1·C22** 입찰 전 안내 창 = 한도 계단 + 버튼 아래 고정 · **C18** 후기 개발 표시·계약 연결 |
| 9f3f045 | C15 타임라인 이름 · C21 「착공 확인 확인」 |
| 239ec50 | C34 · B8 · D17 지킬 수 없는 약속·틀린 비율 문구 |
| 726c9e6 | D3 관리자 분쟁·정산 «없음»(조회 실패 숨김) |
| 53d2a26 | D21 관리자 고객 「견적 0건」 |
| dcf1934 | C6 예산 「이하·이상」 · C7 금액 예시 · C8 칩 글 붙음 |
| dc12ca2 | C4 성실견적 XP 과장 · C5 업적 토스트 반복 |
| cc88f4f | C2 고객 홈 「진행 중」 카드 단계 |
| 52aa10e | **C27** 사진 기록 실패 시 멈추고, 고객에게 안 넘어간 걸 화면에 분명히 |
| 816415b | **C24** 위치 없이 진행한 단계 → 고객 화면에 「위치 기록 없이 진행 · 업체가 남긴 사유」 |
| 6933d89 | **D14** 완료 탭에서 연 공사 화면 업체명 「—」(`getCompanyByIdOrOwner` + 계약 행 업체) |
| c2b8762 | **C19** 「계약·공사 기록」 완료 공사를 완료로 + 「공사 기록 보기」 버튼 · 다른 공사의 선택값이 섞이지 않게 |
| 6b1d755 | **C11** 관리자 역할 홈이 빈 화면 → 관리 화면으로 |
| 165bc7c | **C3** 최종 견적서 보낸 즉시 진행 카드 「최종견적 검토중」 |
| 95ae3ac | **C23** 업체가 입찰하면 업체 본인에게 뜨던 고객용 팝업 제거 · 「30%가 업체에 안전하게 지급됐습니다」 같은 틀린 지급 비율 알림·토스트 문구 정리 |
| 244eb94 | **SQL 113** `chats.read_at` 추가(운영에 없음 → 안읽음 조회·읽음 처리 매번 400) + `payment_fee_rules` 표(404) |

### 판단만 한 것
- C13(보호 금액 261)은 계산상 맞을 가능성 큼(290−29, 고객 이용료 제외) — 배포 뒤 화면으로 재확인.
- C10(최종 견적 칩 순서)은 #713 전 경로 때문일 가능성 — 배포 뒤 확인.

---

## 3. 아직 안 된 것

### 3-1. 이 브랜치에서 마저 할 것 (코드)
1. **라운지 신고가 관리자에게 안 간다** — `ReportModal.jsx` 가 신고를 **신고한 사람 브라우저(localStorage)** 에만 저장, 관리자 화면은 운영에 없는 `lounge_reports` 표를 읽음(404). 「신고됐어요」라고 하지만 아무도 못 본다(없는 기능 약속). → 113 에 `lounge_reports` 표 추가(앱은 Supabase 로그인 세션이 없으니 **insert 는 security definer RPC** 로, 읽기는 관리자 RPC) + ReportModal 이 DB 로 보내게. 스키마 원본은 `supabase/schema.sql` 915행.
2. 운영에 없는 표(코드 호출처 없음 → 급하지 않음): `contract_notes` · `contract_scopes` · `customer_reports` · `webhook_logs` · `identity_verification_log`(102 미실행이라 정상) · `fee_config`(안 씀).
3. **C14 · C32** 파트너센터 숫자(정산 예정·입금 예정·에스크로 N% 지급·이번 달 수익)가 단계·A3 계획과 안 맞음 → `escrow_stage_plan`(112) 기준 한 계산으로.
4. **C20 · D15** 숫자 불일치(공간온도 70°/71°, 완료 0건/5건/6건, 포트폴리오 사례 0 ↔ 시공사례 17건).
5. **D16** 지역 지도 업체 카드(후기 0.0 · 시공 0 · Lv.1 · 확인 안 된 공간보증 칩) — C1 과 같은 뿌리, 지도 쪽 정규화만 남음.
6. **C33** 대화방: 조사 「테스트업체을(를)」, 시스템 줄 시각 없음, 공사 중인데 빠른 답장이 계약 전 질문.
7. **C9** 파트너 홈 「김태웅님」 ↔ 파트너센터 「테스트업체 님」.
8. **C12/C25 남은 콘솔 오류** — 로그인 상태로 한 바퀴 돌며 다시 수집(113 실행 뒤).
9. 알림함(종)은 역할로 안 거른다 — 한 번호에 세 역할인 대표 계정에서만 섞여 보임. 필요하면 알림 type → 역할 표로 거르기.
10. 발견만 한 문구: `SafePaymentScreen.jsx:87` 「업체에 안전하게 지급됩니다」(정식 화면인지 확인 후).

### 3-2. 대표 결정이 필요한 것
- **B9 · B10 · B11 · C31** — 공사 화면 「공간마켓이 보관 중」·「토스페이먼츠 에스크로」·「○○만원 보호 중」, 홈 「공간안전결제 · 정식 오픈 예정」, 리뷰 「커피쿠폰 지급」. 「정식처럼」 결정 ↔ 토스 테스트 키 · 「보관 약속 금지」 규칙이 부딪힘. 선택지: (가) 흐름·문구 모두 정식 / (나) 흐름은 정식, 돈을 실제로 맡기 전까지 문구는 베타.
- **A4** 고객 번복 → 매너온도 반영 방식 · **A5** 선택 전 「상담」 버튼 · **A6** 현장방문 단계.

### 3-3. 대표가 할 것
- SQL **113 실행**(원문은 파일 그대로, 끝의 확인 쿼리가 둘 다 `true` 면 끝).
- 점검 데이터 정리 SQL(`docs/sql/cleanup-check-2026-09-24-b.sql` — D20 점검 후기가 공개 홈에 노출).
- 머지 지시(「머지해」).

---

## 4. 앞으로 할 것 (추천 순)

1. **3-1 의 1번(라운지 신고)** 마무리 → 113 에 합치고 원문을 대표에게.
2. 3-1 의 3~8번 순서대로 수정·커밋(이 브랜치).
3. worktree 에서 **`npm test` · `npm run build`** (node_modules 이미 설치됨) → 푸시 → PR → 대표에게 「머지해」 요청 + 113 원문.
4. 배포 뒤 **로그인해서 한 바퀴 재점검**(의뢰인·파트너·관리자): C10 · C13 · C17(결제 뒤 ←) · C19 · C24 · C27 · D14 화면 확인, 콘솔 오류 재수집.
5. 3-2 대표 결정 받아 B9~B11 · C31 · A4~A6.
6. **힉스필드 재구성**(기준 = 「내 한도 · 서류」, 깊은 초록 `#0E2B1D`·아이보리·금 엠블럼): ①최종견적서 확인 ②결제 수단 선택 ③파트너센터(남색·이모지) ④성실견적 분석 창 ⑤최종견적 도착 ⑥현장방문 완료 ⑦대화방 공사 카드·단계 기록 카드·빈 방.
7. **점검·수정이 모두 끝난 뒤에만** 앱 빌드: 스토어 1.0.20(v5 아이콘) · 스토어 이미지 재촬영(「예시」 표시) · ASO 문안(후보는 점검 문서).
8. 그 뒤: 본인인증 포트원(키 3개 → 102) · iOS EAS/TestFlight/APNs · D-U-N-S 회신 · documents 버킷 비공개 · 토스 지급대행 · 법무 확인.

---

## 5. 방법 · 함정

- **작업 폴더**: `D:\project\gonggan-fix-0924c`(worktree, 브랜치 `fix/double-pay-guard`). 메인 폴더 `D:\project\gonggan-market` 은 다른 창 브랜치·커밋 안 된 파일 → 손대지 않는다. worktree 에서 맨 `git stash` 금지.
- git: `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0='*'` (전역 설정 안 바꿈). 커밋 메시지는 스크래치 폴더 파일로(BOM 없이). **메시지 파일을 `D:\project` 에 만들지 말 것.**
- 파일 편집: CRLF 파일이 섞여 있다 → 파이썬 `io.open(newline="")` 로 읽고 `\n`→`\r\n` 맞춰 바꾸기. Windows 파이썬은 `/tmp` 를 못 읽는다 → 스크래치 절대경로.
- **React 훅은 early return 위에**(React #300). 컴포넌트 안에서 **나중에 선언된 state 를 앞 훅의 의존성에 쓰면 TDZ 오류** — D14 때 한 번 걸렸다(별도 훅으로 뺌).
- 운영 스키마 확인은 브라우저에서 anon 키로 `rest/v1/<표>?select=*&limit=0` — 404 PGRST205 = 표 없음, 400 42703 = 칸 없음. (RPC 를 틀린 인자로 부르면 늘 PGRST202 라 존재 확인에 못 씀.)
- 운영에 없는 칸: `requests.updated_at` · `bids.selected` · `companies.phone` · `chats.read_at`(113 전). `bids.company_id` 는 업체 ID 일 수도 주인 사용자 ID 일 수도 → `resolveCompanyId` / `getCompanyByIdOrOwner`.
- Vercel 서버리스 12개 한도 — 새 `api/` 파일 금지(기존 파일에 분기).
- 역할 전환: 마이(하단 마지막) → 「로그아웃」 → 「무료 비교견적 받기」 → 「다시 오셨네요」 역할 버튼(글자로 찾기). 인증이 풀리면 번호·인증번호는 대표.
- GPS 점검은 실제 위치 대신 강서구 고정 좌표(37.5509, 126.8495)로 geolocation 바꿔서.

## 6. 규칙 (계속 유효)

- 비밀번호·OTP·인증번호·결제 정보·결제창·캡차는 대표. 약관 동의는 대표 허락 뒤에만.
- 머지·배포·스토어 업로드·운영 SQL 실행은 대표가 말할 때만(SQL 은 파일·원문으로 주고 실행은 대표).
- 가짜 파트너 계정 로그인 금지(운영 DB 에 업체 행이 생김). 로컬 확인은 쓰기를 막은 미리보기로.
- 생성 이미지에 가짜 글자·간판·앱 화면 금지, 예시 업체엔 「예시」.
- 베타 동안 «공간마켓이 보관» 같은 보관·에스크로 약속 금지 — 없는 기능을 약속하지 않는다(메모리 `gonggan-no-fake-claims`).

---

## 진행 로그 (끊기면 여기부터 — 항목마다 한 줄씩 추가)

- 09-24 밤 · `046ba58` 라운지 신고 → 서버(113 ③ lounge_reports + RPC 3개) · 관리자 라운지 탭이 서버 목록을 읽음 · 실패 시 「접수됐어요」 안 띄움. 테스트 165 · 빌드 OK. **113 본문 대표에게 전달함(실행 여부 확인 필요).**
- 대표 질문(보류 중): 사업자 없는 업체 300만원 미만 = 2단계 vs 완료 후 100% → Claude 권장 «완료 후 100% 유지 + 「전액 예치됨·48시간 자동 확정·사업자 내면 착공 30%」 안내». 대표 답 대기.
- 대표: 「빌드는 나중에」 「점검이 계속 끊기니 잘 해 달라」 「자체 브라우저로 보여 주면서」 → 운영(내부 브라우저, 모바일 크기)에서 역할별로 한 바퀴 돌며 확인, 항목마다 이 로그에 한 줄.
- 09-24 밤 · 대표: **SQL 113 실행 — 확인 셋 다 true**(chats.read_at · payment_fee_rules · lounge_reports). 내부 브라우저는 새로 열 때마다 기기 인증이 풀린다 → 로그인은 대표.
- 09-24 밤 · 2차 거래(290만원)는 이미 정산완료(다른 창). → **3차 점검 거래를 새로 만든다**(오늘 날짜, 서류 없는 테스트업체 = A3 규칙상 «완료 때 100%» 가 나와야 함). 내부 브라우저 세 역할 모두 기기 인증됨 — 역할 전환은 window.__switch('업체'|'의뢰인'|'관리자') (페이지 새로고침하면 다시 정의).
- 09-24 밤 · **대표 결정: A안 = 「입찰·상담은 누구나, 계약은 사업자부터」**(선택되면 사업자등록 안내 → 승인 뒤 계약, 기한·관리자 알림). 서류 없는 업체 «완료 100%» 예외는 없어짐. 구현은 3차 점검 거래를 끝낸 뒤(이 브랜치). 원격이 넘긴 1·3번(대표 인건비·제도권 편입)은 점검 뒤 답변.
- 3차 거래: 의뢰인 요청(아파트 부분·20평대·도배·예산 200~250) → 업체 입찰 240만원·3일 완료. 입찰 전 안내 창은 운영에 옛 문구(B1, 이 브랜치에서 고침).
- 09-24 밤 · 3차 거래: 선택 → 예약 확인 화면 **「완료 확인 100% · 240만원」 = A3 운영 작동 확인** · 방 열림·카드 「업체 선택됨 · 현장방문 조율」 ✅. 자동 승인 72h 문구는 #713 전 옛 기록(지금은 서버 48h 읽음). `2eb8ee3` C33 구분선·시각·단계별 빠른 문장.
- 09-24 밤 · 3차: 파트너센터 카드 계약 전 「정산 예정 240·입금 예정 240·에스크로 0%·D-30(3일 공사)」 = **C14 재현**. 최종 견적 칩 순서 도배 7번째 = **C10 재현**(고칠 것). 최종 견적 → 방 기록 「240만원·3일」 ✅(#711). 예약 확정 RPC **200** ✅. 결제 화면 249만원(240+9) + A3 안내 ✅. **결제는 대표 차례.**
- 09-24 밤 · `85bb79a` 대표 지시 «서로 존중 문구» — 고객 공간온도 서버 반영이 없어서 **SQL 114**(고객 평가·선택 뒤 번복) 먼저 만들고 문구 3곳. **114 원문 대표에게 전달**. 대표 결제 대기 중.
- 09-24 밤 · **가상계좌 결제 실패** — 토스 sandbox `px-payment-parameters` 401. 운영 클라이언트 키가 토스 문서 공용 샘플 키(`test_ck_D5GePW…`) → 대표 상점 테스트 키 한 쌍(클라이언트+시크릿)으로 Vercel 교체 필요(대표). 결제 화면 기본·추천이 가상계좌라 운영에선 치명 → 키 교체 후에도 안 되면 가상계좌 「준비중」+카드 기본. 이번 점검은 카드로.
- 09-24 밤 · 대표 114 실행 → 42703(18행) = **운영에 users.space_temp 칸 없음**(schema.sql 에만). 114 맨 앞에 add column 추가. 고객 「공간온도 36.5°」는 그동안 기본값이었다. **카드 결제도 실패** — 같은 sandbox 401(공용 샘플 키) → 키 교체 전엔 결제 점검 불가.
- 09-24 밤 · 대표 **114 실행 — 셋 다 true**(users.space_temp 생김, 고객 36.5° 시작). `11eca57` **C10** 고침. 결제: 공용 샘플 키 401 → 대표에게 ①내 테스트 키 / ②VITE_SAFE_MODE=true(정식 전 반드시 끔) 중 선택 요청.
- 09-24 밤 · `c1ca146` **C14** 파트너센터 돈 숫자(lib/partnerMoney · 시험 4). 테스트 170 · 빌드 OK.
- 09-24 늦은 밤 · 대표 「여기서 멈추고 인계지시서」 → **4차 인계지시서 `docs/HANDOFF-2026-09-24-e2e-4.md`** 로 이어짐.
- 09-24 (5차 창) · 토스 문서 확인: 「결제창형 연동 키는 전자결제 신청 이후에만」 → 상점이 안 열려 ①내 테스트 키 불가, 결제 점검 보류. 로그인 상태로 운영 한 바퀴(의뢰인·업체): 네트워크 4xx 0건. 의뢰인 마이 숫자 일치(완료 3=공간 이력 3). 업체 홈 **D15 · C9 운영 확인** → 고침(아래 커밋).
- 09-24 (5차 창) · **D16** 지도 업체 카드 「후기 0.0」 — companies 에 평점 칸이 없고 목록이 없는 row.rating 을 읽었다. 목록을 부른 뒤 공개 후기를 한 번에 모아 평균·개수(lib/reviewStats · 시험 3). 운영 anon 으로 같은 쿼리 200 · 테스트업체 5·5 확인. 공간보증 칩은 C1(isGuaranteeBadgeVisible) 머지로 같이 바로잡힌다. 테스트 173 · 빌드 OK.
- 09-24 (5차 창) · 🔒 **보안 — 내부 쓰기 함수가 누구에게나 열려 있었다.** security definer 인데 revoke 가 없어 anon 키로 company_temp_add·customer_temp_add(아무 업체·고객 공간온도 조작)·company_recount_completed·token_balance_lock·request_notify_partners 를 부를 수 있다. 운영 확인: 없는 ID·변화량 0 으로 customer_temp_add → **204**(데이터 변화 없음). **SQL 115** = 다섯 revoke(부르는 쪽 전부 definer 트리거라 흐름 무변) + 고객 공간온도 읽기 `user_space_temp`. 앱: 의뢰인 마이가 그 값을 읽는다(115 전엔 기본값 그대로). **115 실행은 대표.** 남은 설계 문제(메모): token_earn·token_spend 는 앱이 부르는 RPC 라 잠글 수 없고 p_user_id 를 클라이언트가 준다 — 서버 세션 없이는 «남의 이름으로 적립»을 못 막는다.
- 09-24 (5차 창) · 대표 115 실행(6칸 true) → 운영 anon 으로 company_temp_add·customer_temp_add **401** 확인. **#718 머지·배포** → 운영 확인: 업체 홈 「테스트업체님 · 완료 6건」 · 시장 줄 없음 · 지도 5.0 · 공간보증 칩 사라짐 · 파트너센터 정산 예정 0 · 입금 예정 — · 「계약 전 · 최종 견적·결제 대기」 · 이번 달 545만원. 고객 공간온도 RPC 200(저장값도 36.5 — 평가 전). 새 브랜치 `feat/contract-needs-biz`.
- 09-24 (5차 창) · XP 30 차이(지도 780/1200 ↔ 파트너센터 810) — 파트너센터·메인 성장카드는 옛 공간보증 기준(등급만 있어도), 지도는 C1 기준. 셋 다 isGuaranteeBadgeVisible 로, 레벨 추정(TrustEmblems)의 후기 가산도 표준(완료+공간보증)에 맞춰 뺌.
- 09-24 (5차 창) · **A안 구현(계약은 사업자부터)** — lib/contractGate(시험 3) · 결제 화면 버튼 「업체 사업자 확인 대기 중」(서버 1STEP 또는 verified=false) · **결제 승인 서버가 승인 전에 막음(BIZ_REQUIRED 409 → 매입 안 됨)** · 파트너센터 안내+「사업자등록증 올리기」 · 문서센터 사업자등록증 승인 → companies.verified 켬(빈틈이었다: 승인해도 안 켜졌음) · 알림 라우팅 3종 · 파트너 문구 4곳(입찰은 가입만, 계약은 사업자 뒤). **SQL 116**(selected_at · 선택 시 업체·관리자 알림 · 확인 시 의뢰인 알림 · 72시간 지난 미확인 업체 번복은 온도 차감 없음) — **116 먼저 실행 후 배포.** 테스트 195 · 빌드 OK.
