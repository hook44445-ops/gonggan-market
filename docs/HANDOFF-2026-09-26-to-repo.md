# 저장소 Claude 인계지시서 — 2026-09-26 (로컬 Claude → 저장소 Claude)

받는 사람: 저장소(원격) Claude. 대표는 김태웅, 호칭 「대표」. **보고는 모두 한국어.**
기준: `docs/HANDOFF-2026-09-26-e2e-9.md`(9차) 4장. 이 문서는 그중 **저장소에서 코드·문서로 끝낼 수 있는 일**만 뽑았다.
로컬에서만 되는 일(내부 브라우저 운영 점검 · 대표 로그인 · 힉스필드 그림 · 스토어 빌드 · 대표 SQL 실행 확인)은 로컬 Claude 가 한다.

## 0. 로컬이 방금 확인한 것 (09-26, 운영 · 김태웅 계정)

| 항목 | 결과 |
|---|---|
| SQL 134 | 대표 실행 · 확인 칸 3개 true |
| L3 토큰 화면 | ✅ DB 값 30 · 내역(적립·사용) 보임 — 가짜 20 사라짐 |
| L1 프로필 팝오버 「메시지 신청」 | ✅ 「타일러 님에게 메시지」 같은 시트 · 실제 잔액 30 |
| L5·L6 이미 열린 상대에게 신청 | ✅ 새 요청 없음 · 토큰 추가 차감 없음 · 수락된 방 `12e7390c` 로 이동 |

## 1. 할 일 (추천 순)

### R1. 토큰 내역 「--20」 (작음)
- `src/screens/TokenHistoryScreen.jsx:59` `{isEarn ? '+' : '-'}{log.amount}` — 대화 수락 차감(`accept_lounge_chat`)은 원장에 **음수(-20)** 로 쓰고 111 `token_spend` 는 양수로 쓴다 → 「--20」.
- 표시를 `Math.abs(log.amount)` 로. (원장 부호 통일은 하지 말 것 — 기존 행이 섞여 있다.)

### R2. 대화 신청 시트 — 이미 열린 방이면 미리 알리기 · 적은 메시지 버리지 않기
- 지금: 이미 대화 중인 상대여도 시트가 「수락하면 20토큰 차감」이라 말하고, 메시지를 적어야 보낼 수 있다. 서버가 `already_accepted` 를 돌려주면 방으로 가지만 **적은 메시지는 버려진다**.
- 할 것: 시트를 열 때 두 사람 사이 수락된 방이 있으면 시트 대신 「이미 대화 중이에요 — 대화방 열기」 한 줄(토큰 문구 없음). 서버 판정(134)은 그대로 두고 화면만 미리. 이미 시트에서 보냈다면 적은 메시지를 그 방의 첫 메시지로 보낸다.
- 위치: `src/screens/LoungePostDetailScreen.jsx:714~750` · `src/components/lounge/ChatRequestModal.jsx` · `src/components/MainApp.jsx:4640` 근처(스토리 경로) · `LoungeMyPageSection.jsx:329`.
- 9차 4-2 첫 줄(스토리·프로필 팝오버 경로를 같은 시트로)도 함께.

### R3. 라운지 대화방 → 견적 요청 흐름 (대표 요청 · 9차 4-1-2)
로컬이 운영에서 눌러 본 결과:
- 방 머리 「📋 견적요청」(`src/screens/ChatScreen.jsx:494`) → **대화방을 떠나 홈**으로 가고 그 위에 베타 안내 확인 창(「내용을 확인했어요」 체크)이 뜬다.
- 요청서에 **어느 대화·글에서 왔는지가 붙지 않고**, 요청 뒤 **대화방으로 돌아오는 연결이 없다**. 상대가 업체여도 그 업체에게 가는 길이 없다.
설계 제안(대표 확인 받을 것):
1. 방 안에서 요청 시작 → 요청서 첫 칸에 「라운지 대화에서 이어진 요청」 표시(`requests` 에 출처 칸 또는 설명 첫 줄), 제출 뒤 **대화방에 시스템 줄**(「견적 요청을 올렸어요 · 요청 보기」).
2. 상대가 승인 업체면 「이 업체에게도 견적 받기」 선택지(일반 공개 요청 + 해당 업체 알림). 개인 간 직거래 유도로 보이지 않게 — 견적·계약은 앱 안.
3. 베타 안내 창은 요청 제출 순간 한 번(이미 동의한 사용자는 생략).

### R4. 대화방 UI·UX (대표 요청 · 9차 4-1-3)
- 같은 화면(`ChatScreen.jsx`, 라운지 `mode="lounge"`). 로딩 중 머리에 「🔒 익명 · 수락 전」이 잠깐 보였다가 사라진다(수락된 방) — 상태를 불러오기 전엔 표시하지 말 것.
- 힉스필드 그림이 필요한 자리는 **목록만** 적어 로컬에 넘긴다(저장소는 힉스필드 도구가 없을 수 있다). 기준: 점토 톤 · 깊은 초록 · 금(`/images/empty/*.webp` 가족).

### R5. 라운지 토큰 화면 — 결제 전 문구 (9차 4-2)
- 로컬 확인: `TokenStoreScreen` 이 결제 불가 상태인데 「토큰 충전 · 구매 즉시 지급 · 결제는 토스페이먼츠로 안전하게」를 그대로 보여 준다. 마이의 「보유 토큰 30 · 충전」 버튼도.
- `PAYMENTS_LIVE` 가 아니면: 「충전」 → 「모으는 방법」, 무료 미션·고객센터(070-7954-2740)를 먼저, 구매 목록은 「정식 오픈 뒤」 표시(누를 수 없게).

### R6. SQL 이 만든 알림도 푸시 (9차 4-3 첫 줄 · 결제 열기 전 필수)
- `escrow_auto_approve_due()`(112) · `request_notify_partners`(110 은 push_logs 직접 넣음 — 이건 됨) 등 **SQL 이 `notifications` 에 직접 넣는 알림은 푸시 큐(`push_logs`)에 안 들어간다** → 48시간 자동 승인 「12시간 전 경고」가 푸시로 안 감.
- 제안: `notifications` AFTER INSERT 트리거 → `push_preferences` 확인(enqueue.js `decidePushGate` 와 같은 규칙) → `push_logs` 에 queued. 앱 `createNotification` 경로와 **중복되지 않게**(enqueue 는 같은 user·type·related_id 중복을 막는다 — 트리거에서도 같은 기준). 새 마이그레이션 번호는 main 최신 다음 번호. 전문은 대표에게(실행은 대표).
- 알림 종류를 새로 만들면 `src/utils/notify.js` 와 `api/push/enqueue.js` 둘 다 등록(9차 5장).

### R7. E20 — 에스크로 표 직접 쓰기 닫기 (결제 열기 전 필수 · 131 뒤)
- 8차 4-3 5번 그대로: `approveEscrowPayoutByStage`·`advanceContractStep`·`markEscrowPhaseStarted`·`setEscrowPayoutReady`·`createEscrowPayoutsForContract`·`holdAllPayoutsForEscrow` 를 사용자 확인하는 definer 함수로 옮기고 `ALL:public` 정책 닫기.
- **131(관리자 문 잠그기)은 대표 로그인·토큰 확인이 필요해 로컬이 한다** — E20 코드·SQL 은 저장소에서 준비만(실행 순서: 131 → E20).

### R8. 결제 금액 서버 검증 (결제 열기 전 필수 · 새로 적음)
- 지금 토스 결제 금액을 앱이 정하고 `/api/confirm-payment` 는 그 금액 그대로 승인한다. 금액을 조작하면 적게 내고 계약 금액 전체로 에스크로가 생길 수 있다.
- 제안: 결제창 열기 전 서버 함수가 (요청·선택 입찰·최종 견적·이용료 규칙)으로 금액을 계산해 주문(`payment_orders` pending, order_id)을 만들고, confirm 은 order_id 로 금액을 대조. 에스크로 생성도 그 주문 금액 기준.

### R9. 프루비 · 공간마켓 · 공간사이 SEO·GEO·AEO 자체 마케팅 시스템 — 설계 문서 먼저 (9차 4-1-5)
- 코드보다 `docs/` 설계 문서. 출발점: 라운지 구조(생성기 → 한 줄 답·FAQ → prerender 구조화 데이터 → `llms.txt`·sitemap, #764). 세 서비스 공통 파이프라인 · 각자 채널 · 측정(검색 노출·AI 인용).

### R10. 원격이 넘긴 3건 답변 (9차 4-5)
- 원문 `docs/HANDOFF-2026-09-24-to-local.md` §8. 대표 결정: **2번은 A안(계약부터 사업자 필수)**, 지금은 입찰부터 사업자 필요(SQL 124)로 더 앞에서 막힘. 1번(대표자 인건비)·3번(제도권 편입 논지, 과장 금지)은 저장소에서 문서로 정리해 대표에게.

## 2. 로컬이 계속 하는 것 (저장소는 손대지 않음)
- 운영 화면 확인(위 R 들이 배포된 뒤) · 대표 로그인 필요한 점검 · 131 실행 확인 · AI 글 작동 점검 SQL 결과 판단(9차 0장 ②) · 김태웅 +20 되돌리기·[점검] 댓글 2개 삭제(대표 결정 뒤) · 힉스필드 그림 · 스토어 1.0.20 빌드 · Play 테스터 12명.

## 3. 규칙 (9차 6장 그대로)
- 머지·배포·운영 SQL 실행은 대표가 말할 때만 — SQL 은 전체 원문 + 확인 칸.
- 없는 기능 약속 금지(보관·에스크로·지급·충전 문구는 `PAYMENTS_LIVE`). 남의 계정에 영향 주는 조작 금지. SQL 122 실행 금지.
