# 인계지시서 — 공간마켓 09-23 ~ 09-24 (총점검까지)

받는 사람: 다음 세션 Claude. 대표는 김태웅, 호칭은 「대표」.
작업 폴더: `D:\project\gonggan-market` · 운영 https://gongganmarket.com
앞 인계지시서: `docs/HANDOFF-2026-09-24-premium-partner.md`(프리미엄 파트너 시스템) — 이 문서가 그 뒤를 잇는다.
점검 상세: **`docs/CHECK-2026-09-24-e2e.md`** (찾은 것 전부 · 힉스필드 보정 목록 · 스토어 이미지 후보 · ASO 사실)

## 0. 지금 상태

| 항목 | 상태 |
|---|---|
| main | `93e36bd` — PR #704 · #705 · #706 · #707 전부 머지·배포됨 |
| 커밋 안 된 파일 | `docs/CHECK-2026-09-24-e2e.md`, 이 문서, `store/DUNS-followup.md`, `store/HANDOFF-2026-09-24-push-icon.md`(낡음 표시) |
| 운영 DB 101(수주 한도) | 실행됨 · 앱과 숫자 같음 |
| 운영 DB 102(진짜 본인인증) | **미실행** — 포트원 키 3개를 Vercel 에 넣은 뒤 대표가 실행 |
| 운영 DB 점검 데이터 | 「[점검]」 표시 — 요청 1(아파트 부분·20평대·강서구) · 입찰 1(테스트업체 280만원) · 현장방문 요청 · 최종 견적서 1. 정리 SQL 은 대표가 실행 |
| 토스 | 운영 키가 **테스트 키**(`test_ck_…`) — 실제 돈은 안 나간다 |

## 1. 대표가 한 말 (09-24, 이 창)

- 푸시: 「종이·에스크로·라운지 채팅 등 iOS·구글앱 푸시 연결할 곳들 연결해」「관리자 페이지까지」
- 아이콘: 「녹색이 아동복지 같다, 프리미엄 인테리어 비교견적(중개) 느낌이 아니다」「새로 만들 거면 힉스필드와 같이」「기본 아이덴티티 봐봐」「서로 보기 좋게 상담하는 모습이 좋은데 아동복지 느낌만 없으면」「그냥 현재에 배경색만 바꾸는 방향은?」「현재는 랜딩 왼쪽 상단」**「더 깊은 초록이나 네이비로」**
- 「머지」「머지 후 진행」「그리고 머지」「머지해」 — 인계지시서 할 일을 추천 순으로, 내부 브라우저로 확인
- 한도(에스컬레이션): 「1,000만원 초과부터는 보증금 들어가는 거 아님?」「에스컬레이션이 헷갈릴 수 있는 부분 정리」**「일단 1안 위주, 2안은 문의 시로. 2안이 생기면 기본 규정이 헷갈려짐」「쉽게 가면 1,000만원까지 시공보험·사업자등록증으로 커버, 그 이상은 보증금 프리미엄 파트너」「에스컬레이션 타면서」「가입 문턱을 낮춰서 에스컬레이션으로 가면 된다」**
- 화면: 「구조·구성·배경 및 UX/UI 까지 힉스필드로」
- 총점검: 「의뢰인 입장에서 견적 신청부터 에스크로 결제·리뷰 후기까지. 파트너는 받아 주면서. 관리자 아이디로. 시스템 총점검」「너가 직접 사용하면서 총평」「01027406030 계정 — 관리자·의뢰인·파트너 다 들어가 있어」「로그아웃하고 다시 들어가면 돼」
- 「총점검하면서 스토어 이미지와 ASO 에 넣을 만한 내용까지 보관」「힉스필드 구조 보정해야 할 곳들도 점검」「최종견적서 확인 쪽도 힉스필드로 구조적으로 보기 좋게」「결제 수단 선택 구간도」
- **「베타라도 정식 서비스 하는 것처럼 진행」** ← 마지막 결정

## 2. 한 것 (PR 순)

- **#704** (다른 창 작업 포함) 프리미엄 파트너 시스템 · 푸시 배선(수신설정 매핑 16종 · 즉시 발송 · 관리자 푸시 현황) · 아이콘 v4
- **#705** 베타에서 공사비 «보관·에스크로» 약속 문구 20여 곳을 `SHOW_BETA_UI` 분기로 · 관리자 「수동 인증」 제거(102 전후 동작 동일) · **아이콘 v5** = 로고(집·마주한 두 사람·열린 문) 그대로, 배경만 깊은 초록 `#0E2B1D` — 랜딩 로고·웹 매니페스트·파비콘·안드로이드 런처·iOS 아이콘까지(Play·ASC 등록 아이콘은 그대로)
- **#706** 면허 서류 칸(`interior_license`, 승인 → `license_verified`) · **「내 한도 · 서류」 화면**(지금 한도 → 다음 한 칸 → 한도 계단 → 서류 3묶음, 힉스필드 엠블럼 `license`·`join` + 배경 `limit/hero.webp`) · `normalizeCompany` 가 면허를 빠뜨려 입찰 한도가 면허를 못 보던 버그
- **#707** 총점검 치명 버그 3건(「이 업체로 선택」 앱 멈춤 React #300 · 파트너센터 입찰 탭 입찰 불가 · 진행중 조회 42703) + 한도 계단 정리(1안만 안내 · 베이직/스탠다드 보증금은 한도 못 올림 · 프리미엄 파트너 = 1,000만원 초과 가능 업체 · 보증금 안내는 걸 수 있는 공간보증 등급으로)
- 총점검: 요청 → 입찰 → 비교·선택 → 현장방문 → 최종 견적서 작성·전송 → 의뢰인 확인까지 직접. **결제에서 멈춤**(결제창은 Claude 가 조작하지 않음)

## 3. 확정된 정책

- **수주 한도(에스컬레이션)**: 가입만 300만원 → 사업자등록증 500만원 → + 시공보험 1,000만원 → **1,000만원 초과는 보증금(공간보증 200만원 이상) = 프리미엄 파트너**, 1,500만원 미만 → + 실내건축공사업 등록증이면 보증금 × 10, 최대 1억원.
  - 화면은 1안 하나만. 2안(보험 없이 보증금 20%)·사업자 없이 보험/보증금 500만원은 계산(앱·서버 101)에만 남기고 **문의 시** 관리자가 처리.
  - 가입 문턱은 낮게 — 업체명·지역·공종만, 서류는 원할 때 하나씩.
  - 숫자는 `src/lib/partnerTier.js` 한 곳, 서버 101 과 같다.
- **로고·아이콘**: 두 사람 로고 + 깊은 초록 `#0E2B1D`(대안 네이비). 그림은 바꾸지 않는다.
- **베타 운영**: 정식 서비스처럼 흐름을 돌린다 — 결제 단계 유지.

## 4. 아직 안 된 것 · 앞으로 할 것 (추천 순)

1. **「정식처럼」 결정과 문구 규칙 맞추기 — 먼저 대표 확인.**
   #705 에서 베타 문구(「대금은 계약서 단계대로 업체와 직접」)로 바꾼 곳과, 결제 화면이 그대로 있는 흐름이 부딪힌다.
   선택지: (가) `VITE_APP_MODE` 를 정식으로 돌려 흐름·문구를 모두 정식으로 / (나) 흐름은 정식, 문구는 «실제로 돈을 맡기 전까지» 베타 유지.
   ⚠ CLAUDE.md 규칙: 베타 동안 「공간마켓이 보관」 같은 보관·에스크로 약속 금지 — 토스가 테스트 키인 동안 (가)는 거짓 약속이 된다. 대표가 정하게 한다.
2. **결제 이후 점검 이어가기**: 결제(대표가 테스트 결제창 직접) → 공사 진행 → 단계 승인(착공·중간·완료) → 정산 → 후기. `docs/CHECK-…` 의 표에 이어 적는다.
3. **힉스필드 재구성**(기준 = 「내 한도 · 서류」 화면 가족): ①최종 견적서 확인 ②결제 수단 선택 ③파트너센터 전체(남색·이모지) ④성실견적 분석 창(파란색) ⑤최종 견적 도착 화면 ⑥현장방문 요청 완료 화면.
4. **입찰 전 안내 창**(B1): 「가입 필수: 사업자등록증·시공보험·준수서약」·「공간보증 = 정식 오픈 때」 → 한도 계단 기준으로.
5. **문구 B2~B8** — 1번 결정에 따라 정리(입찰 알림 창 「에스크로 안전 정산」, 현장방문 완료 「안전결제」, 최종 견적 도착 「공사 안전 결제·에스크로로 결제」, 확정 버튼 「결제 진행」, 결제 화면 제목·본문 모순, 파트너 카드 「에스크로 0% 지급·입금 예정」, 홈 「검증된 업체 3곳 1분」·「2~4시간 내 연락」).
6. **버그 C1~C12**(점검 문서): 특히 C1 의뢰인 비교 목록의 업체 정보 기본값(「선택된 파트너」·36.5°·Lv.1·확인 안 된 「✓ 공간보증」 칩), C2 홈 카드가 최종 견적 도착을 안 알림·계약 전인데 「진행 중인 공사」, C4 성실견적 +77 XP 과장, C5 파트너센터 들어갈 때마다 「첫 후기」 업적.
7. **D1 첫 경험**: 「무료 비교견적 받기」 → 요청서를 먼저 쓰고 보내는 순간 전화 인증.
8. 본인인증: 포트원 가입·채널 → Vercel 환경변수 3개(`VITE_PORTONE_STORE_ID` · `VITE_PORTONE_IDENTITY_CHANNEL_KEY` · `PORTONE_API_SECRET`) → 102 실행 → 실제 인증창 시험 → 업체 카드 「본인인증」 표시.
9. 스토어: 이미지 재촬영(예시 데이터 · 「예시」 표시 · 후보는 점검 문서) · ASO 문안 새 기준 · **1.0.20(v5 아이콘)** 빌드·업로드 + Play 아이콘 교체(1.0.18 검토 끝난 뒤) · ASC 프로모션 텍스트 확인 후 저장.
10. iOS: `D:\project\gonggan-ios` EAS 빌드 → TestFlight → APNs 키(대표) → `expo-notifications`.
11. 나중: 점검 데이터 정리 SQL · documents 버킷 비공개 · 토스 지급대행 · 법무 확인(1,500만원·면허 요건) · D-U-N-S 회신(애플 케이스 102973436873) · 정부지원사업 사업계획 · OCR·국세청 API.

## 5. 방법 · 함정

- **역할 바꾸기**: 로그아웃 → 첫 화면 「고객/파트너」 → 「다시 오셨네요」 → 계정 선택(의뢰인·업체·관리자). 이 기기(내부 브라우저)는 인증돼 있어 문자 없이 들어간다. 인증번호는 대표가 넣는다.
- **체크아웃 전에 Vite 서버를 끈다.** D: 는 exFAT 이고, 개발 서버가 `public/` 을 붙잡고 있으면 git 이 지운 폴더가 «삭제 대기»로 남아 「Permission denied」가 난다.
- 미리보기: `preview_start` 이름은 `gonggan-market`(D:\project\.claude\launch.json, 포트 5180). 운영 DB 를 안 건드리는 확인은 임시 페이지(`limit-preview.html` + `src/__preview/`)로 — 확인 뒤 지운다.
- 힉스필드: `gpt_image_2_5` + 기존 엠블럼을 참고 이미지로 올리면 한 가족으로 나온다. 받은 그림은 반드시 열어 가짜 글자가 없는지 본다(soul_2 는 가짜 글자를 자주 넣는다).
- PNG 뽑기: 이 PC 엔 SVG 변환기가 없다 — 브라우저 canvas 로 그려 localhost 저장 서버로 보낸다(`https` 페이지에서 `http://localhost` 는 막히므로 로컬 페이지에서만). `C:\WINDOWS\system32\convert` 는 ImageMagick 이 아니다 — 실행 금지.
- 훅 순서: 컴포넌트에서 `if (…) return` 뒤에 훅을 두면 React #300. 점검 문서 날짜의 커밋 메시지에 훑는 방법이 있다.
- 파이썬은 Windows 판 — `/tmp` 를 못 읽는다. 큰 수정 스크립트는 Write 도구로 스크래치패드에 만든다(셸 heredoc 따옴표 문제).
- Vercel 서버리스 함수 12개 한도 — 새 `api` 파일 금지.

## 6. 규칙 (계속 유효)

- 비밀번호·OTP·인증코드·D-U-N-S·API 키·결제 정보는 Claude 가 입력하지 않는다. 결제창 조작도 대표.
- 머지·배포·업로드·제출은 대표가 말할 때만. 운영 DB SQL 은 파일로만, 실행은 대표.
- 생성 이미지에 가짜 글자 금지. 예시 업체에는 「예시」.
- 커밋 메시지는 BOM 없이 파일로. git 은 `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0='*'`.
