# 인계지시서 10차 — 2026-09-26 (9차에 이어서)

받는 사람: 다음 세션 Claude. 대표는 김태웅, 호칭은 「대표」. **보고·안내는 모두 한국어.**
운영 https://gongganmarket.com
앞 문서: … → 9차 `docs/HANDOFF-2026-09-26-e2e-9.md`(#765) → **이 문서(10차, 최신)**.
9차 5·6장(방법·함정·규칙)은 그대로 유효 — 먼저 읽어라. 이 문서 5·6장은 그 위에 더한 것.
작업 폴더: worktree `D:\project\gonggan-fix-0924c`. **메인 폴더 `D:\project\gonggan-market` 은 건드리지 않는다.** 새 일은 origin/main 에서 새 브랜치.
저장소 Claude 용 인계: `docs/HANDOFF-2026-09-26-to-repo.md`(#766) — 대표가 09-26 「나누지 말고 지금은 같이 진행」이라 R1~R10 대부분을 로컬이 끝냈다(아래 3장).

---

## 0. 새 창에서 제일 먼저

| # | 할 일 | 누가 |
|---|---|---|
| ① | **관리자 페이지 정리 이어가기**(대표 지시, 4-1). 브랜치 `fix/admin-page` 에 첫 수정(토큰 조정 원장) WIP 커밋 `dc74b29` 가 있다 — 이어서 쓰고 PR. **정리안(메뉴 구조)을 먼저 대표에게 보여 주고** 확인 뒤 만든다 | Claude |
| ② | 관리자 토큰 조정 수정이 배포되면 **김태웅 +20 토큰 되돌리기**(9차 0장 ③, 대표 「진행해」 받음) — 관리자 「공간토큰 수동 관리」에서 010-2740-6030 · +20 · 사유 「L6 중복 차감 되돌림」 | Claude(대표 로그인된 내부 브라우저) |
| ③ | **[점검] 댓글 2개 정리**(「고객이 바로 이해하는 견적서는…」 글의 타일러·홈디자이너 댓글) — 대표 「진행해」 받음. **영구 삭제가 아니라 숨김/삭제 표시(is_deleted)** 인지 먼저 확인하고 그 방식으로만 | Claude |
| ④ | **AI 글 작동 점검 SQL 결과** — 9차 4-1 ④ 조회, 대표에게 드렸고 아직 못 받음 | 대표 |

---

## 1. 지금 상태 (여기서 멈춤)

| 항목 | 상태 |
|---|---|
| main | **#766 · #767 · #768 · #769 머지·배포** (마지막 `6e9d29a`) |
| 운영 SQL | **134 · 135 · 131 · 136 · 137 실행·확인 끝**(아래 3장). 122 계속 실행 금지 |
| 결제 열기 전 필수 | **131(관리자 문) ✅ · E20(에스크로 표 직접 쓰기 닫기) ✅ · SQL 알림 푸시(135) ✅ · 결제 금액 원 단위 ✅**. 남은 것: 토스 상점 개설 → 새 점검 거래로 결제~착공~완료 전체 흐름 확인(E20 뒤 새 방식으로는 아직 안 돌려 봄 — 예상 완공일 저장만 운영 확인) |
| 내부 브라우저 | 대표가 로그인해 둔 상태(김태웅, 토큰 있음 · `session_whoami` → is_admin true). 탭 `seed` |
| 테스트 | 225 통과 |
| 메일 회신 대기 | 9차 그대로(D&B #34875982 · Apple 102973436873 · 토스) |

---

## 2. 대표가 한 말 (09-26, 9차 뒤)

- 「로컬 클로드에서만 진행할 수 있는 걸 진행한다, 저장소 클로드에서 할 수 있는 건 따로 저장소 클로드 인계지시서로」 → #766 작성. 이어서 **「저장소 지시서는 바로바로 줘」**(원문을 대화창에 복붙용으로).
- 곧바로 **「아니다 그냥 같이 지금은 진행해」** → 저장소 몫(R1~R10)도 로컬이 진행.
- R3(라운지 대화 → 견적 요청): **「추천대로 해」**.
- 「관리자 페이지 사용하기 쉽게 보기 좋게 구조적으로 정리할 것, 필요시 힉스필드와 함께」 · 「문제 있는 곳은 수정할 것」 · **「관리자 페이지 정리부터」**.
- +20 토큰 되돌리기·[점검] 댓글 정리: **「진행해」**.
- 「나에게 보고할 땐 한글로 보고해」(메모리 `report-in-korean`).
- 토스 재문의 메일 초안 요청 → 초안 드림(발송은 대표).

---

## 3. 9차 뒤 한 것

| PR · SQL | 내용 | 운영 확인 |
|---|---|---|
| **134** | 토큰 잔액·내역 서버 함수(L3) · 대화 «이미 열린 방» 양방향(L6) | ✅ 토큰 화면 DB 값 30 · 내역 / 이미 대화 중인 상대 → 새 요청 없이 기존 방 |
| #766 | 저장소 Claude 인계지시서 R1~R10 | — |
| #767 · **135** | R1 토큰 내역 「--20」 · **R5 결제 전 토큰 구매 차단**(운영 테스트 키로 실제 토큰이 적립될 수 있던 구멍) · 「충전」→「모으기」 · 상품·가격 목록은 유지(토스 심사 페이지) · R2 이미 대화 중이면 시트가 「대화방 열기」, 적은 메시지 버리지 않음 · 스토리 작성자 대화 신청도 같은 시트 · R4 방 머리 「수락 전」 깜빡임 · **R3 라운지 대화 → 견적 요청**(요청서 첫 줄 출처 · 제출 뒤 방에 시스템 줄 · 상대가 승인 업체면 알림 · 약관 확인은 «보내기» 순간) · **135 SQL 알림 6종 푸시**(자동 승인 경고·자동 승인·사업자 72시간·결제 가능·관리자 2종) · **결제 금액 원 단위**(공사대금이 만원 숫자 그대로 토스로 갔다 — 서버 금액 검사에 모두 거절될 뻔) | ✅ R2·R4·R5 운영 확인 |
| #768 | **자체 마케팅 시스템 설계**(SEO·GEO·AEO, 공간사이·공간마켓·프루비) `docs/MARKETING-SYSTEM-SEO-GEO-AEO.md` | 문서 |
| **131** | 관리자 문 잠그기(관리자 함수는 관리자 토큰만) | ✅ 관리자 화면 9개 호출 200 |
| #769 · **136 · 137** | **E20** — 에스크로 3표(escrow_payments·escrow_payouts·phase_photos) 앱 직접 쓰기 12곳 → 서버 함수 `escrow_action`(업체 보고·고객 승인·이의 신청·완공일·지급 줄·되돌리기) · `phase_photos_add` · 관리자 3종. 당사자 = 로그인 토큰 사용자(한 사람이 고객·업체 둘 다여도 됨). 137 로 세 표 쓰기 정책 닫고 읽기만 | ✅ 완공일 저장 `escrow_action` 200 · 137 뒤 새로고침 읽기 정상 |

**R8 결제 금액 서버 검증은 6차에서 이미 있었다**(`api/confirm-payment`: gm_ 주문 · contract_base_price 이상 · 중복 결제 · 사업자) — 로컬이 쓴 SQL 136(결제 intent)은 버렸다. 번호 136 은 E20 이 썼다.

---

## 4. 안 된 것 · 앞으로 할 것

### 4-1. 관리자 페이지 정리 (진행 중 · 대표 최우선)
**관찰(운영, 09-26):**
- 상단 탭 **10개**가 가로로 넘친다: 운영 · 거래 · 프로젝트증빙 · AI 운영센터 · 편성국 · 발행센터 · 무인운영 · AI 분석실 · 라운지·리뷰 · 시스템. 그 아래 하위 탭이 또 3~7개.
- 대시보드 첫 화면 순서: 방문자 수 → 오늘 지표 → 거래액 추세 → **서류 확인 대기(테스트업체 통장사본 106일째)** → 현황 요약 → 수수료 표 → 긴급 운영 스위치. **«지금 처리할 일»이 네 번째에 묻혀 있고 긴급 스위치는 맨 아래.**
- 헤더 **「홈으로」 눌러도 반응 없음**(대시보드에서). 「아이콘 v2」「화면 v3」 토글은 개발 흔적.
- **「공간토큰 수동 관리」가 앱이 안 쓰는 `users.space_tokens` 칸을 고쳤다** → `fix/admin-page` WIP 에서 `api/admin/users.js` 를 `space_tokens`·`space_token_logs` 로 고침(아직 PR 전). 고객관리 목록의 토큰 숫자(`AdminScreen.jsx:4129 row.space_tokens`)도 원장 기준으로 바꿔야 한다.
- 거래 안에 거래관리·결제관리·정산관리·분쟁관리, 시스템 안에 **재무대시보드**, 프로젝트증빙 안에 증빙관리·GPS 시스템 모니터링·GPS 신뢰도·증빙 타임라인 — 같은 데이터가 여러 곳.
- AI 쪽 탭 5개(AI 운영센터·편성국·발행센터·무인운영·AI 분석실)가 운영 탭과 같은 줄에 섞여 있다. 편성국은 「현재 Mock 데이터」.
- 이모지 아이콘 다수(📝💬📸🚫🛰️🧠❤️🤖).
**코드 지도 결과(09-26, Explore 조사 — 코드 기준, 운영 DB 정책은 저장소 migration 기준 추정):**
- 「홈으로」 원인: `MainApp.jsx:4641` `onHome → setScreen("home")` 인데 `MainApp.jsx:3010` 효과가 «관리자면 home → admin» 으로 즉시 되돌린다. 하단 탭도 admin 화면에선 숨김(3105) → 나가는 길은 「←」(→ 마이)뿐.
- 구조: 상단 10개(`CATEGORIES_DEF` AdminScreen 5918~5939) · 하위 키 `MAIN_TABS`(5866~5909) · 상태 `mainTab`(기본 dashboard, 5367) · 상단 선택은 `AdminCategoryNav.jsx` 안의 `activeCat`. 권한 `adminCategories`/`canAccessTab`(5945·5959).
- **131 뒤 조용히 깨진 것(가장 급함):** 토큰은 `admin_*`·당사자 RPC 에만 붙는다 → **표 직접 읽기·쓰기는 anon** 이라 관리자 정책에 막힌다.
  - 읽기(빈 화면): `getPaymentOrders`(결제관리) · `getDirectDealReports`(직거래 의심·신고관리) · `getAdminLogs`(관리자로그·채팅관리) · `getAdminLoungeChatRequests` · 리뷰 쿠폰(`getReviewRewardsPending`, 확인 필요).
  - 쓰기(실패·0행): `adminSetCompanyStatus` · `adminUpdateCompanyInfo` · `adminUpdateUserInfo` · `adminVerifyUserIdentity` · `adminUpdatePaymentOrder` · `adminRestoreRequest` · `updateDirectDealReportStatus` · `updateReviewReward` · `adminHideContent` · **AI 초안 생성·수정·삭제(`adminCreate/Update/DeleteLoungeDraft` — 발행센터·AI 탭 전부)** · 라운지 댓글 숨김·복구 · 모든 `admin_logs` insert.
  - 고칠 방향: 관리자 화면의 표 직접 접근을 **토큰 연결(`authedDb`)** 로 보내거나 `admin_*` RPC 로. 한 번에 하는 방법: 관리자 화면에서 쓰는 supabase 호출을 `adminDb()`(토큰 연결) 로 바꾸기 — 운영 DB 정책(107 `is_admin()` 등)은 auth.uid() 로 관리자를 알아본다.
- 그 밖에 깨진 것: 라운지관리 「공간온도 조정하기」 `adminAdjustSpaceTemp` import 누락(ReferenceError, 659) · 신고 「숨김」·차단 수는 이 브라우저 localStorage 에만 저장(586~603) · 대시보드 결제/분쟁/정산 대기 타일은 그 탭을 열기 전엔 0 · 여러 탭 새로고침 없음(`tabLoaded`, 5553) · AI 탭들은 「AI 콘텐츠 공장」을 한 번 열어야 데이터가 찬다 · 리뷰 쿠폰 SENT/취소가 실패해도 성공 표시 · 결제관리 「지급 보류」가 실제로는 CANCELLED · 알림 클릭 시 읽음 처리 안 됨 · PIN 로그인 운영자는 131 뒤 관리자 함수 전부 막힘(토큰 없음).
- 보안 빈틈: `ops_config_set`(108)·`op_set_post_hot`/`op_set_post_hidden` 이 `p_actor_id` 를 믿는다(토큰 아님, 131 목록 밖) · `seed_lounge_posts` 정책 `using(true)` 쓰기 열림(004).
- 겹침: 돈 화면 5곳(거래관리 · 재무대시보드(시스템 아래) · 정산 거래별 · 정산 단계별 · 대시보드 KPI — 정산 상태 원천이 둘) · `admin_project_flow_list` 를 9곳이 따로 불러옴 · 직거래 신고 3곳 · 토큰·온도 조정 2곳(고객관리·라운지관리) · 자동발행 4곳 · «AI 본부» 4곳 · 「리뷰」 이름 둘(쿠폰 vs 관리) · «운영» 이름 셋.
- 개발 흔적: 아이콘 v2·화면 v3 토글 · [DEV] 패널 · 푸시 테스트 발송 · 정리도구(영구 삭제) · 실전 검증(`E2EValidation` 은 실제로 글을 발행) · `LiveOps`(탭 열려 있으면 자동 발행) · `OperationMonitor`(60초 루프).
- 첫 화면에 없는 매일 할 일: 푸시 현황(시스템›알림 안) · 파트너상담 대기 수 · 직거래 의심·분쟁 수.

**진행 방법:** 코드 구조 지도(Explore 에이전트로 조사 시작했다 — 새 창에서는 다시 조사) → **정리안을 대표에게 먼저**(예: 첫 화면 «오늘 할 일» — 서류 심사·분쟁·신고·정산 대기·긴급 스위치 / 상단 4~5개로 묶기: 오늘 · 업체·고객 · 거래·돈 · 라운지·신고 · 콘텐츠 AI(한곳으로) · 설정 / 겹치는 화면 합치기 / 개발 토글 숨기기) → 확인 뒤 구현 → 빈 화면·아이콘은 힉스필드(점토 톤·깊은 초록·금, 글자 없는 그림).
- ⚠ 관리자 쓰기는 이제 관리자 토큰이 필요(131) — 직접 표 쓰기(`supabase.from(...).update`)가 남아 있으면 실패한다. 정리하면서 `admin_*` RPC 또는 `api/admin/*` 로.

### 4-2. 라운지·대화 (작은 것)
- 대화방 UI·UX 힉스필드 가족으로(9차 4-1-3) — 필요한 그림 자리 목록부터.
- 토큰 스토어 「무료 미션」 탭 첫 화면 정리(결제 전엔 미션이 주인공).

### 4-3. 결제 길이 열리면 (토스 상점 개설 뒤)
토스 키 교체 → `VITE_APP_MODE=production` → 새 점검 거래로 **결제(원 단위 금액 확인) → 착공 보고(escrow_action) → 승인 → 중간 → 완료 → 후기 · GPS · 48시간 자동 승인 푸시(135) · 2/3/4STEP 지급 기록** 전체 확인.

### 4-4. 그대로 남은 것 (9차에서)
- AI 글 작동 점검(0장 ④) → 마케팅 설계 0단계.
- 마케팅 시스템 1단계: gonggansai.com · 프루비 랜딩·스토어 현황 확인, 이름 표기 통일(`docs/MARKETING-SYSTEM-SEO-GEO-AEO.md` 7장).
- 원격 3건 답변(R10 — `docs/HANDOFF-2026-09-24-to-local.md` §8): 1 대표자 인건비 · 3 제도권 편입 논지는 문서 정리 필요, 2는 A안 결정.
- Play 테스터 12명(인테리어 업체 12곳) · 스토어 1.0.20(로고 v6) · 포트원 본인인증 · iOS(D-U-N-S).
- 토스 재문의 메일 — 초안 드림, 대표 발송 여부 확인.

---

## 5. 방법 · 함정 (9차 뒤 새로 배운 것)

- **운영 확인은 내부 브라우저 화면으로.** 대표가 09-26 **직접 API 호출(콘솔에서 fetch 로 관리자 함수 부르기)을 한 번 거절**했다 — 확인은 앱 화면 조작으로 한다. 운영 번들에 새 코드가 들어갔는지 확인(정적 JS 읽기)은 괜찮았다.
- **배포 확인**: `gh api repos/hook44445-ops/gonggan-market/commits/<sha>/statuses` 의 Vercel 상태가 success 가 된 뒤 운영 번들에 새 문자열이 있는지 본다. 머지 직후엔 «pending» 이다 — **SQL 순서가 배포에 걸려 있으면(예: 137) 반드시 배포 확인 뒤에** 실행하라고 말할 것.
- **토큰(로그인 세션)**: `localStorage.gonggan_session_tokens[userId]`. 앱은 관리자 함수(`isGuardedRpc`)와 당사자 함수(`isTokenRpc`: escrow_action · phase_photos_add)만 토큰 연결로 보낸다(`src/lib/supabase.js` rpc 래퍼).
- **관리자 → 다른 역할**: 관리자 화면 「←」 → 마이 → 로그아웃 → 「무료 비교견적 받기」 → 역할 버튼. 「홈으로」는 지금 동작 안 함.
- **점검 계정은 한 사람이 세 역할** — 서버 판정은 역할을 «각각» 봐야 한다(136 `_escrow_party` 가 jsonb 로 customer·company·admin 을 따로 준다).
- **토스 결제 금액 단위**: 앱 금액은 만원(소수 1자리), 토스는 원 → `Math.round(만원 × 10000)`. 복귀 처리는 토스가 돌려준 원 단위 amount 로 승인.
- **운영이 토스 테스트 키**인 동안 결제창이 열리면 테스트 결제가 «성공»한다 → 결제 전(`PAYMENTS_LIVE` 아님)에는 구매·결제 입구 자체를 막는다.
- 복잡한 파이썬 편집은 **스크래치 폴더에 .py 로 Write 한 뒤 실행**(bash heredoc 한국어·따옴표 깨짐 방지). 스크래치: `C:\Users\hook4\AppData\Local\Temp\claude\D--project\d4826b11-…\scratchpad`.
- 빌드 확인: 워크트리엔 node_modules 가 없다(이 드라이브는 junction 불가) → `NODE_PATH=D:/project/gonggan-market/node_modules` + 메인 폴더의 `esbuild`로 번들, 테스트는 `node --test <package.json test 목록>`.

## 6. 규칙 (9차 6장 그대로 + 새것)
- 보고는 한국어 · SQL 은 **전체 원문 + 확인 칸 수**(대표는 「○○ 원문 줘」로 받는다) · 저장소용 지시서는 원문을 바로 대화창에.
- 머지·배포·운영 SQL 실행·메일 발송은 대표가 말할 때만.
- **영구 삭제 금지**(댓글·데이터 정리는 숨김/삭제 표시 방식만) · 남의 계정에 영향 주는 조작 금지 · 가짜 글자 그림 금지 · 없는 기능 약속 금지(`PAYMENTS_LIVE`).
- 메인 폴더 손대지 않기 · 워크트리에서 맨 `git stash` 금지 · git 은 `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0='*'` · SQL 122 실행 금지.

---

## 7. 10차 뒤 (09-26 오후) — 한 것 · 외부 API 키 붙이기

### 7-1. 한 것 (전부 머지·배포)
| PR · SQL | 내용 | 운영 확인 |
|---|---|---|
| #771 · **138** | 관리자 페이지 1차 — 관리자 표 접근 토큰 연결(`adminDb`) 34곳 · 토큰 조정 원장(`space_tokens`·`space_token_logs`) · 홈으로 제거 · 신고 숨김 서버 · 쿠폰 결과 · 알림 읽음 · 138 운영자 함수 토큰 사용자로 · seed 쓰기 관리자만 | 138 결과는 아직 못 받음 |
| #772 · **139** | 관리자 공지 푸시(`AdminPushBroadcast`) · 라운지 새 글 푸시 서버 트리거(`lounge_news`) · 인기(HOT) 글 푸시(`lounge_hot`) | ✅ 139 true 3개 |
| #773 · **140** | AI 운영 안전 1단계 — 브라우저 자동 발행 3곳 끔(`BROWSER_AI_AUTOPUBLISH=false`) · Morning Brief 프롬프트 제거(사설 지어냄) · 서버 초안 `ai_source=server_template` 만 자동 승인 · 예약 KST · 예약 발행 하루 15건 · check-trends 크론 인증 | ✅ 140 true |
| #774 · **141 · 142** | 라운지 공감(`lounge_post_like` — 한 사람 1번) · 조회수·댓글 수 서버 권한 · 본인 글 삭제 토큰 본인만(남의 번호로 삭제 막음) · 내 공감 표시 수정 · **142 로 `lounge_posts: anon update` 닫음** | ✅ 141 true 4개 · 142 true 2개 |
| #775 | **라운지 실제 트렌드·뉴스·날씨 글** — `src/lib/trendSources.js`(가져오기) · `src/lib/newsPosts.js`(글 만들기, 테스트 있음) · 자율 사이클 `runNewsStep` · 본문 링크 표시(`richText`) | 오늘의 트렌드 첫 발행 12:30 확인 필요 |

### 7-2. 외부 API 키 붙이기 — 대표 몫(대표 「API 붙이는 건 인계지시서에 남겨 두셈」)
코드는 다 들어가 있다(#775). **키가 없으면 그 글만 안 만들고 넘어간다**(`not_configured`). **Vercel 환경변수는 새 배포부터 읽힌다 → 키를 넣은 뒤 Redeploy 한 번**, 그다음 자율 사이클(5분 간격)부터 켜진다.

| 글 | 키(Vercel › Settings › Environment Variables, Production) | 받는 곳 | 비용 |
|---|---|---|---|
| **공간 뉴스**(하루 1건 18:30 · 집과 닿는 기사만) | `NAVER_CLIENT_ID` · `NAVER_CLIENT_SECRET` | developers.naver.com › Application 등록 › 사용 API 「검색」 · 웹 서비스 URL `https://gongganmarket.com` | 무료(하루 25,000회) |
| **날씨와 집**(내일 한파·폭염·비 예보일 때 17:30) | `KMA_SERVICE_KEY` (**일반 인증키 Decoding** 값) | data.go.kr › 「기상청_단기예보 조회서비스」 활용신청(자동승인, 반영까지 1~2시간) | 무료 |
| 오늘의 트렌드(매일 12:30) | 필요 없음 — Google 트렌드(한국) RSS | — | — |

- **키는 대표가 넣는다**(Claude 는 키를 보거나 입력하지 않는다).
- 넣은 뒤 확인: 자율 사이클 응답/로그의 `news` 칸 — `{"trend":"scheduled|done_today","news":"scheduled","weather":"scheduled|calm_day"}`. `not_configured` 면 키 이름·배포 확인, `no_items` 면 그날 조건에 맞는 기사/예보가 없던 것.
- 날씨는 서울 격자(60,127) 하나. 지역을 늘리려면 `fetchKmaTomorrow({ nx, ny, place })` 를 지역별로.
- **쓰지 않는 것**: 구글 뉴스 RSS — 피드에 「개인·비상업 용도만」. 라운지는 영업의 일부라 상업으로 본다(대표 확인 09-26). 구글 트렌드 RSS 는 명시 금지 문구 없음·출처 표시 — 법률 검토는 아님. 불안하면 네이버 키 뒤 트렌드도 네이버 쪽으로.
- 지키는 선(`newsPosts.js`): 기사 본문 안 옮김(제목·링크·언론사만) · 사건·사고·정치·도박 키워드 제외 · 한글 기사 붙은 키워드만 · 3개 미만이면 안 씀. 발행되면 139 트리거가 라운지 새 글 푸시(하루 3건·10~21시).

### 7-3. 대표 정정(09-26) — 기억할 것
- 공간라운지의 **인테리어 아닌 콘텐츠(큐티·점성술·뉴스)는 빼지 않는다** — 트렌드 요소로 라운지 유입을 여러 각도로(매일 습관 · 참여형 · 시즌 · 우리 데이터 · 검색·공유 · 사람 글 유도 · 프루비 잇기 · 측정). 메모리 `gonggan-lounge-nontopic-inflow`.
- **토스는 열려야 하는 것** — 「결제 오픈 전」 안내를 해법으로 내지 않는다. 결제 열기에 서버에 없는 것: 결제 취소·환불 API · 입금 통보(웹훅) · 업체 실제 지급(지금은 상태만) · 에스크로/지급대행 계약 여부(대표·토스 결정).
- 대표 질문 대기: 「보물을 찾고 파는 기능」 — A(집 속 물건·남은 자재 사고팔기) / B(앱 안 보물찾기 이벤트) 중 무엇인지.

### 7-4. 함정 — 작업 폴더
- **`D:\project\gonggan-fix-0924c` 에서 다른 창의 개발 서버(vite 5183·5187)가 돈다.** 09-26 그 폴더에서 옛 `main` 으로 잘못 체크아웃했다가 되돌리려니, 그 서버가 잡고 있는 폴더(`docs/sql` · `public/images/empty` · `public/images/landing`)가 «삭제 대기» 상태로 잠겨 체크아웃이 막혔다. 그 서버들이 꺼지면 풀린다.
- 그래서 이후 작업은 **새 워크트리 `D:\project\gonggan-fix-0926`**. 다른 창이 쓰는 폴더에서는 브랜치를 바꾸지 않는다.
