# Vercel 배포에서 제외 — 여기 매칭되는 파일은 업로드/함수화되지 않는다.
# 테스트 파일은 엔드포인트가 아니다. /api 안에 있으면 Vercel 이 Serverless Function 으로
# 잘못 집계해 Hobby 플랜 함수 개수 한도(12개)를 소모하므로 제외한다(로컬 node --test 는 그대로 동작).
**/*.test.js
**/*.spec.js
